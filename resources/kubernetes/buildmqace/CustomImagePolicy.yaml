apiVersion: securityenforcement.admission.cloud.ibm.com/v1beta1
kind: ClusterImagePolicy
metadata:
  name: mqace
spec:
   repositories:
    - name: image-registry.openshift-image-registry.svc:5000/mq/mqace
      policy:
        va:
          enabled: false
